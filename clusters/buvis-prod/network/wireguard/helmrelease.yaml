---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: wireguard
  namespace: network
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://place1.github.io/wg-access-server
      chart: wg-access-server
      version: v0.4.6
      sourceRef:
        kind: HelmRepository
        name: wg-access-server
        namespace: flux-system
      interval: 5m
  values:
    config:
      wireguard:
        externalHost: ${SVC_WIREGUARD_ADDRESS}
        web:
          service:
            type: "LoadBalancer"
            loadBalancerIP: ${SVC_WIREGUARD_ADDRESS}
    wireguard:
      config:
        privateKey: ${SECRET_WIREGUARD_PRIVATE_KEY}
        adminPassword: ${SECREAT_WIREGUARD_ADMIN_PASSWORD}
      service:
        type: "LoadBalancer"
        loadBalancerIP: ${SVC_WIREGUARD_ADDRESS}
      persistence:
        enabled: true
      ingress:
        enabled: true
        hosts: ["vpn.buvis.net"]
        tls:
        - hosts: ["vpn.buvis.net"]
          secretName: "tls-wg-access-server"
